# logitrack-frontend

Next.js 15 (App Router) + shadcn/ui operator, customer and driver dashboards for LogiTrack.

Scaffolded in **Phase 4**. This repo currently holds governance files only so that
branch protection and CI can be configured ahead of the application code.